我想为Chromium系的浏览器做一个AI插件。它的形式就是提供一个侧边栏的AI对话框。可以接入各大AI厂商的api。主要功能是借助大模型的能力帮助用户为当前页面做包括不限于以下的事情：
1. 总结当前页面的内容
2. 可以辅助用户理解当前页面，例如解释某些名词等
3. 自动生成脚本注入当前页面，例如去除当前页面的广告，修改背景颜色，进入阅读模式等等
4. 基于第3点，可以将某些固定的操作固化为skill，这个侧边栏具有管理skill的功能
5. 完成某些"轻量"的针对网页的自动化任务，例如批量抓取当前页面的数据，图片，视频等。常规脚本可能会被反爬机制阻挡，但是利用浏览器插件也许可以做到。